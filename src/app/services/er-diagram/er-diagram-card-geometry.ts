/** Shared geometry used by both layout inputs and the rendered table cards. */
export const ER_DIAGRAM_CARD_GEOMETRY = Object.freeze({
  width: 280,
  headerHeight: 44,
  rowHeight: 32,
  emptyBodyHeight: 32,
  contentPaddingX: 12,
});
